import React, { Component } from 'react';
import ListItem from './ListItem';

class List extends Component {
  // --- Render ---
  render(){
    return(
      <div>
        List
      </div>
    );
  }
}

export default List;
