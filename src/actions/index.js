/*
// Action description //////////////////////////////////
export const actionName = (argument1, argument2) => {
	return function(dispatch) {
		// BUSINESS LOGIC HERE
		dispatch({                      // wywolujemy dispatch kiedy chcemy. Najczesciej w ostanim .then w przypadku async function
			type: 'type_name',
			payload: 'wartosc zwracana przez action creator'
		});
	};
};
*/
